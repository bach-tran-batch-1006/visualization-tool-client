

export interface Project{
    projectId?: number;
    projectName: string;
    // projectDesc?:string;
    // isActive: boolean;
}

export interface ProjectDTO {
    // projectId?: any;
    projectName: string;
    // projectDesc?:string;
}